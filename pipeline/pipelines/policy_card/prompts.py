"""
pipeline/pipelines/policy_card/prompts.py

변경사항:
  - _EVALUATE_CORE_PROMPT, _EVALUATE_PROS_PROMPT 제거
    → 각 팀 Supervisor 프롬프트에 편향+품질 판단 통합
  - 평가 기준 현실화: 300자 이상 + 편향 없음 + 창작 없음 → 통과
  - _CORE_PROMPT: search_context 파라미터 추가
  - _PROS_ONLY_PROMPT → _PROS_PROMPT로 이름 통일
  - _CONS_ONLY_PROMPT → _CONS_PROMPT로 이름 통일
"""

# ── 사실 추출 ──────────────────────────────────────────────────────────────
_EXTRACT_POLICY_PROMPT = """\
다음 정책/법안 문서에서 카드 작성에 필요한 핵심 사실만 추출하세요.
문서에 없는 내용은 절대 추측하거나 창작하지 마세요.

[문서]
{policy_doc}

JSON 응답:
{{
  "name":         "정책/법안명",
  "main_purpose": "주요 목적 (2-3문장)",
  "target":       "지원/적용 대상 (없으면 null)",
  "content":      "지원/규제 내용 (없으면 null)",
  "period":       "신청/시행 기간 (없으면 null)",
  "method":       "신청 방법 (없으면 null)",
  "contact":      "문의처 (없으면 null)",
  "background":   "도입 배경 (없으면 null)",
  "numbers":      ["언급된 수치/통계 — 단위 포함"],
  "entities":     ["관련 기관명/부처명"],
  "key_points":   ["핵심 포인트 3-5개"]
}}"""

_EXTRACT_ARTICLE_PROMPT = """\
다음 기사에서 카드 작성에 필요한 핵심 사실을 추출하세요.

[기사]
언론사: {publisher}
제목: {title}
본문: {content}

JSON 응답:
{{
  "publisher":  "언론사명",
  "main_claim": "핵심 주장 (1-2문장)",
  "numbers":    ["언급된 수치 — 단위 포함"],
  "entities":   ["언급된 기관명/인물명"],
  "key_points": ["핵심 포인트 2-3개"],
  "stance":     "찬성|반대|중립 및 근거 (1문장)"
}}"""

# ── SUMMARY 탭 ──────────────────────────────────────────────────────────────
_SUMMARY_PROMPT = """\
다음 정책/법안 사실을 바탕으로 SUMMARY 탭 내용을 JSON으로 작성하세요.

[정책/법안 핵심 사실]
{policy_facts}

[절대 원칙] 위 사실에 없는 내용 창작/추측 금지. 없으면 "확인 필요"로 표기.

JSON 응답:
{{
  "title":            "카드 제목 (30자 이내, 청년 친화적)",
  "category":         "일자리|교육|주거|금융|생활복지|문화 중 하나",
  "summary_points":   ["핵심 요약 1", "핵심 요약 2", "... (3-5개)"],
  "youth_connection": "청년 일상과의 연관성 (2-3문장, 구체적으로)",
  "policy_details": {{
    "target":  "지원 대상 (없으면 '확인 필요')",
    "content": "지원 내용 (없으면 '확인 필요')",
    "period":  "신청 기간 (없으면 '확인 필요')",
    "method":  "신청 방법 (없으면 '확인 필요')",
    "contact": "문의처 (없으면 '확인 필요')"
  }}
}}"""

# ── CORE 탭 생성 ───────────────────────────────────────────────────────────
_CORE_PROMPT = """\
다음 자료를 바탕으로 CORE 탭(본문)을 작성하세요.

[정책/법안 핵심 사실]
{policy_facts}

[관련 기사 핵심 사실]
{article_facts}

[추가 수집 자료 (웹 검색 결과, 없으면 무시)]
{search_context}

[작성 기준]
- 분량: 원본 자료 범위 내에서 최대한 풍부하게 (최소 1000자)
- 구성:
  ① 사회적 맥락: 
    - 이 정책이 도입되기 전에 어떤 문제가 있었나?
    - 기존 제도(고용보험, 실업급여 등)가 왜 부족했는가?
    - 어떤 계층이 사각지대에 놓여 있었는가?
  ② 정책/법안 내용: 구체적으로 무엇이 바뀌는가?
  ③ 이해관계자: 실제 단체/기관을 주어로 — 누가 어떤 입장인가?
  ④ 생각해볼 것: 핵심 쟁점을 담은 질문 하나
- 원본 자료에 없는 수치·기관명·날짜 창작 절대 금지
- 일상 대화체, 전문 용어는 괄호로 설명

JSON 응답:
{{
  "core_content":        "본문 전문",
  "discussion_question": "토론으로 이어질 핵심 질문 (1-2문장)"
}}"""

# ── CORE 팀 Supervisor — 품질+편향 통합 판단 (evaluate_core 흡수) ──────────
_CORE_SUPERVISOR_PROMPT = """\
당신은 정책 카드 핵심 내용(CORE)의 편집장입니다.

[현재 초안]
{draft}

[원본 팩트 자료]
{policy_facts}
{article_facts}

[수집된 추가 자료]
{search_context}

[판단 기준 — 순서대로 검사하세요]

1. 편향성 (최우선):
   특정 정당·정치인을 지지하거나 비방하는 표현이 있는가?
   → 있으면: "GENERATE", feedback = "편향 제거 후 재작성: [구체적 표현 지적]"

2. 사실 창작:
   원본 자료에 없는 수치·기관명·날짜를 지어냈는가?
   → 있으면: "GENERATE", feedback = "창작 내용 제거: [구체적 내용 지적]"

3. 자료 보강:
   초안이 1000자 미만이고, 원본 자료만으로는 맥락 설명이 부족한가?
   → 그렇다면: "SEARCH", feedback = 검색할 구체적 키워드

4. 통과:
   편향 없고, 창작 없고, 300자 이상이면 → "FINISH"
   자료가 부족해 짧더라도 편향·창작이 없으면 → "FINISH"

현재 반복: {loop_count}/3 (3회 도달 시 무조건 "FINISH")

JSON 응답: {{"next_worker": "SEARCH"|"GENERATE"|"FINISH", "feedback": "지시사항 또는 검색 키워드"}}"""

# ── 찬성 의견 생성 ──────────────────────────────────────────────────────────
_PROS_PROMPT = """\
다음 정책/법안 사실을 바탕으로 찬성 입장을 작성하세요.

[정책/법안 핵심 사실]
{policy_facts}

[관련 기사 핵심 사실]
{article_facts}

[작성 지침]
- 실제 이해관계자(단체명, 수혜 계층)를 주어로 명시하세요 (예: "청년유니온은 ~")
- 구체적인 기대 효과와 이익을 서술하세요
- 원본 자료에 없는 내용 창작 금지
- 최소 200자 이상

JSON 응답:
{{"argument": "구체적 찬성 근거 (200자 이상)"}}"""

# ── 찬성 팀 Supervisor — 편향+품질 통합 판단 (evaluate_pros 흡수) ──────────
_PROS_SUPERVISOR_PROMPT = """\
당신은 정책 카드의 찬성 의견을 검수하는 편집장입니다.

[현재 찬성 초안]
{draft}

[판단 기준 — 순서대로 검사하세요]

1. 편향성 (최우선):
   특정 정당·정치인을 지지하거나 비방하는 표현이 있는가?
   → 있으면: "GENERATE", feedback = "편향 제거 후 재작성"

2. 품질:
   200자 이상이고, 구체적인 이해관계자가 주어로 있는가?
   → 없으면: "GENERATE", feedback = "구체적 이해관계자를 주어로 200자 이상 재작성"

3. 통과:
   편향 없고, 200자 이상이면 → "FINISH"

현재 반복: {loop_count}/2 (2회 도달 시 무조건 "FINISH")

JSON 응답: {{"next_worker": "GENERATE"|"FINISH", "feedback": "지시사항"}}"""

# ── 반대 의견 생성 ──────────────────────────────────────────────────────────
_CONS_PROMPT = """\
당신은 정책의 부작용과 사각지대를 날카롭게 파고드는 비판적 논설위원입니다.
제공된 자료와 추가 수집 자료를 종합해 구체적인 반대 의견을 작성하세요.

[원본 자료]
{policy_facts}
{article_facts}

[추가 수집된 비판/부작용 자료]
{search_context}

[작성 지침]
- "근본적 해결책이 아니다" 같은 추상적 문장 금지
- 소외받는 계층, 역차별 집단, 전문가 우려 등 구체적 이해관계자를 주어로
- 원본 자료에 없는 수치 창작 금지
- 최소 200자 이상

JSON 응답:
{{"argument": "구체적 반대 근거 (200자 이상)"}}"""

# ── 반대 팀 Supervisor — 편향+검색+품질 통합 판단 ──────────────────────────
_CONS_SUPERVISOR_PROMPT = """\
당신은 정책의 부작용을 검증하는 비평가 편집장입니다.

[정책/법안 핵심 사실 (검색 키워드 참고용)]
{policy_facts}

[현재 반대 초안]
{draft}

[수집된 비판 자료]
{search_context}

[판단 기준 — 순서대로 검사하세요]

1. 편향성 (최우선):
   특정 정당·정치인을 지지하거나 비방하는 표현이 있는가?
   → 있으면: "GENERATE", feedback = "편향 제거 후 재작성"

2. 자료 보강:
   초안이 200자 미만이거나 추상적인 멘트만 있는가?
   → 그렇다면: "SEARCH", feedback = 위 정책 사실의 name 필드를 활용한 날카로운 검색 키워드
     (예: '국민취업지원제도 한계 역차별', '청년도약계좌 사각지대 비판')

3. 품질:
   200자 이상이고 구체적 이해관계자가 있는가?
   → 없으면: "GENERATE", feedback = "구체적 이해관계자를 주어로 재작성"

4. 통과:
   편향 없고, 200자 이상이면 → "FINISH"

현재 반복: {loop_count}/3 (3회 도달 시 무조건 "FINISH")

JSON 응답: {{"next_worker": "SEARCH"|"GENERATE"|"FINISH", "feedback": "지시사항 또는 검색 키워드"}}"""